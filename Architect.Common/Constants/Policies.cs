namespace Architect.Common.Constants
{
    public static class Policies
    {
        public const string All = nameof(All);
        public const string Administration = nameof(Administration);
    }
}
